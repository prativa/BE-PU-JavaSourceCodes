/**
 * An interface contains only abstract methods and constants
 * Cannot have instace variables or constructors
 * It defines a contract tha classes must adhere to by implementing its methods
 */

public interface ScholarshipEligible {
    boolean isEligibleForScholarship();
   // void hasStudentPassed(double[] grades, String name);

}
